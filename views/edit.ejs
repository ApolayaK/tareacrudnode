<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Empleado</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <main>
    <section class="container mt-3">
      <form action="/update" method="POST" autocomplete="off">
        <div class="card">
          <div class="card-header">
            <a href="/" class="btn btn-sm btn-secondary">Volver</a>
          </div>
          <div class="card-body">
            <input type="hidden" name="id" value="<%= empleado.id %>">

            <div class="mb-3 form-floating">
              <input type="text" class="form-control" name="nombre" id="nombre" value="<%= empleado.nombre %>" required>
              <label for="nombre">Nombre</label>
            </div>

            <div class="mb-3 form-floating">
              <input type="text" class="form-control" name="direccion" id="direccion" value="<%= empleado.direccion %>" required>
              <label for="direccion">Dirección</label>
            </div>

            <div class="mb-3 form-floating">
              <input type="tel" class="form-control" name="telefono" id="telefono" maxlength="9" value="<%= empleado.telefono %>" required>
              <label for="telefono">Teléfono</label>
            </div>

            <div class="mb-3 form-floating">
              <input type="date" class="form-control" name="fecha_ingreso" id="fecha_ingreso" value="<%= empleado.fecha_ingreso.toISOString().split('T')[0] %>" required>
              <label for="fecha_ingreso">Fecha de Ingreso</label>
            </div>

            <div class="mb-3 form-floating">
              <input type="number" class="form-control" name="faltas" id="faltas" min="0" value="<%= empleado.faltas %>" required>
              <label for="faltas">Faltas</label>
            </div>

            <div class="mb-3 form-floating">
                <select class="form-select" name="cargo" required>
                    <option value="">Seleccione cargo</option>
                    <option value="Diseñador Gráfico" <%= empleado.cargo === 'Diseñador Gráfico' ? 'selected' : '' %>>Diseñador Gráfico</option>
                    <option value="Programador" <%= empleado.cargo === 'Programador' ? 'selected' : '' %>>Programador</option>
                    <option value="Administrador" <%= empleado.cargo === 'Administrador' ? 'selected' : '' %>>Administrador</option>
                </select>                  
                <label for="cargo">Cargo</label>
            </div>
          </div>

          <div class="card-footer text-end">
            <button class="btn btn-sm btn-success" type="submit">Actualizar</button>
            <a href="/" class="btn btn-sm btn-secondary">Cancelar</a>
          </div>
        </div>
      </form>
    </section>
  </main>
</body>
</html>
